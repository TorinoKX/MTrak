import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'au.ZakCD.MTrak',
  appName: 'MTrak',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
